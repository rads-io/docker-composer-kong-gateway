 apis:
  -
    name: "account"
    attributes:
      upstream_url: "http://u-service-account:8080"
      hosts: "api.lokoo.local"
      uris:
        - "/accounts"
      methods: ["POST", "PUT", "GET", "DELETE"]
    plugins:
